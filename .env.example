# =============================================================================
# CONFIGURATION DES VARIABLES D'ENVIRONNEMENT
# =============================================================================
# Ce fichier contient toutes les variables d'environnement nécessaires pour
# l'application TTR Gestion. Copiez ce fichier en `.env.local` et remplissez
# les valeurs appropriées.
# =============================================================================

# -----------------------------------------------------------------------------
# FIREBASE (Obligatoire)
# -----------------------------------------------------------------------------
# Configuration Firebase pour l'authentification et la base de données
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_DATABASE_URL=

# -----------------------------------------------------------------------------
# CLOUDINARY (Obligatoire pour upload d'images)
# -----------------------------------------------------------------------------
# Configuration Cloudinary pour le stockage des images
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=

# -----------------------------------------------------------------------------
# CODES PROMO & AMBASSADEUR (Obligatoire si vous utilisez les codes promo)
# -----------------------------------------------------------------------------
# URL du proxy Cloudflare Worker pour la validation des codes promo
# Cette variable est utilisée UNIQUEMENT côté serveur (API routes Next.js)
# Elle n'est JAMAIS exposée au client
CLOUDFLARE_PROXY_URL=https://proxy.ttrgestion.workers.dev/verify-code

# URL de base de l'API pour les clients (APK, EXE)
# Cette variable est utilisée par les applications APK et EXE pour savoir
# où appeler l'API de la PWA. Pour Web/PWA, elle peut rester vide.
# Exemples :
#   - Production: https://pwa.ttrgestion.site
#   - Développement: http://localhost:9002
NEXT_PUBLIC_API_BASE_URL=

# -----------------------------------------------------------------------------
# GEMINI AI (Optionnel - pour les fonctionnalités IA)
# -----------------------------------------------------------------------------
GEMINI_API_KEY=

# -----------------------------------------------------------------------------
# CONFIGURATION DE DÉPLOIEMENT
# -----------------------------------------------------------------------------
# Nom de l'application (utilisé dans les métadonnées)
NEXT_PUBLIC_APP_NAME=TTR Gestion

# URL de base de l'application
NEXT_PUBLIC_APP_URL=https://app.ttrgestion.site

# Environment (development, production)
NODE_ENV=production

# -----------------------------------------------------------------------------
# NOTES IMPORTANTES
# -----------------------------------------------------------------------------
# 
# 1. Variables NEXT_PUBLIC_* :
#    Ces variables sont exposées au client (navigateur, APK, EXE).
#    NE JAMAIS y mettre de clés secrètes ou d'informations sensibles.
#
# 2. Variables sans préfixe NEXT_PUBLIC_* :
#    Ces variables sont UNIQUEMENT accessibles côté serveur (API routes).
#    Utilisez-les pour les clés secrètes et informations sensibles.
#
# 3. Codes Promo Multi-Plateforme :
#    - Web/PWA : Utilise /api/promo/verify avec URL relative
#    - APK : Utilise NEXT_PUBLIC_API_BASE_URL + /api/promo/verify
#    - EXE : Utilise NEXT_PUBLIC_API_BASE_URL + /api/promo/verify
#
# 4. Architecture de Validation des Codes Promo :
#    Client → API Route TTR → Proxy Cloudflare → API ABT
#
#    Avantages :
#    - Sécurité : La clé API ABT reste sur Cloudflare
#    - CORS : Géré automatiquement
#    - Rate limiting : Implémenté dans l'API route
#    - Logging : Centralisé
#    - Multi-plateforme : Fonctionne sur Web, APK et EXE
#
# 5. Pour le développement local :
#    - Créez un fichier `.env.local` avec vos valeurs
#    - Ne committez JAMAIS le fichier `.env.local`
#    - Utilisez des valeurs de test/développement
#
# 6. Pour la production :
#    - Utilisez les variables d'environnement de votre plateforme
#      (Vercel, Netlify, etc.)
#    - Assurez-vous que CLOUDFLARE_PROXY_URL pointe vers le bon Worker
#    - Vérifiez que NEXT_PUBLIC_API_BASE_URL est correct pour APK/EXE
#
# =============================================================================
